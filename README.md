
  # NeuroCast AI Prototype

  This is a code bundle for NeuroCast AI Prototype. The original project is available at https://www.figma.com/design/V30KaTGUs7whhC79vmVOPl/NeuroCast-AI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  